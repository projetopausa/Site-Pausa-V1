{
  "name": "portal-pausa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && CI=false npm run build",
    "postinstall": "cd frontend && npm install --legacy-peer-deps"
  },
  "engines": {
    "node": "18.x"
  }
}